/** @type {import('next').NextConfig} */
const nextConfig = {
  domain: ['127.0.0.1'],
  images: {
    remotePatterns: [],
    loader: "default"
  },
  reactStrictMode: false
};

export default nextConfig;
